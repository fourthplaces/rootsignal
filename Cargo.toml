[workspace]
members = [
    "crates/ai-client",
    "crates/taproot-core",
    "crates/taproot-domains",
    "crates/taproot-server",
    "crates/twilio-rs",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
futures = "0.3"

# Web framework
axum = "0.8"
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }

# GraphQL
async-graphql = { version = "7", features = ["dataloader", "chrono", "uuid"] }
async-graphql-axum = "7"

# Database
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "uuid", "chrono", "json"] }
pgvector = { version = "0.4", features = ["sqlx"] }

# Durable execution
restate-sdk = "0.4"

# HTTP client
reqwest = { version = "0.12", features = ["json"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = { version = "0.8", features = ["derive"] }

# Error handling
anyhow = "1.0"
thiserror = "2.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Crypto / hashing
sha2 = "0.10"
hex = "0.4"

# Bytes (for Restate SDK serde)
bytes = "1"

# IDs and time
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# Clustering / similarity
strsim = "0.11"
petgraph = "0.7"

# Environment
dotenvy = "0.15"

# JWT
jsonwebtoken = "9"

# Local crates
ai-client = { path = "crates/ai-client" }
taproot-core = { path = "crates/taproot-core" }
taproot-domains = { path = "crates/taproot-domains" }
twilio = { path = "crates/twilio-rs" }
